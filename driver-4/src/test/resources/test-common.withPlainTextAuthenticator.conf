include required(classpath("test-common.conf"))
datastax-java-driver {
  advanced {
    auth-provider {
      class = com.azure.cosmos.cassandra.CosmosAuthProvider
      username = ${?AZURE_COSMOS_CASSANDRA_USERNAME}
      password = ${?AZURE_COSMOS_CASSANDRA_PASSWORD}
    }
  }
}
